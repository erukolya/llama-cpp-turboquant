name: Locate GET_ROWS Types

on:
  push:
    branches:
      - agent/static-moe-expert-placement
    paths:
      - '.github/workflows/moe-locate-get-rows.yml'

permissions:
  contents: write

jobs:
  locate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/static-moe-expert-placement
          fetch-depth: 1

      - name: Locate GET_ROWS implementations
        shell: bash
        run: |
          {
            echo '=== declarations and constructors ==='
            grep -RIn -E 'ggml_get_rows\(|GGML_OP_GET_ROWS' ggml/src ggml/include 2>/dev/null | head -n 500 || true
            echo '=== CPU forward implementations ==='
            grep -RIn -E 'compute_forward_get_rows|get_rows.*GGML_TYPE_I32|GGML_TYPE_I32.*get_rows' ggml/src/ggml-cpu 2>/dev/null | head -n 800 || true
            echo '=== CUDA implementations ==='
            grep -RIn -E 'GGML_OP_GET_ROWS|get_rows' ggml/src/ggml-cuda 2>/dev/null | head -n 800 || true
          } > moe-get-rows-locations.txt

          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add moe-get-rows-locations.txt
          git commit -m 'ci: capture GET_ROWS type support with grep'
          git push origin HEAD:agent/static-moe-expert-placement
